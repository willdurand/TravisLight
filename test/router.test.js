define(
    [
        'router'
    ],
    function (router) {
        describe('router', function () {
            it('should exist', function () {
                assert.ok(router);
            });
        });
    }
);
